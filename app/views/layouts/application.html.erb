<!DOCTYPE html>
<html>
  <head>
    <title>FinalProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <nav class="navbar">
        <div class="navbar-brand">
          <%= link_to "Shards of the Grid", root_path, class: "navbar-logo" %>
        </div>
        <ul class="navbar-links">
          <li><%= link_to "Home", root_path, class: "nav-link" %></li>
          <% if user_signed_in? %>
            <li><%= link_to "Profile", edit_user_registration_path, class: "nav-link" %></li>
            <li>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-link button-link" %>
            </li>
          <% else %>
            <li><%= link_to "Sign In", new_user_session_path, class: "nav-link" %></li>
            <li><%= link_to "Sign Up", new_user_registration_path, class: "nav-link nav-link-signup" %></li>
          <% end %>
        </ul>
      </nav>
    </header>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
